# Олег — платформы

- **ВКонтакте** — личная страница (не группа): https://vk.com/ogp56
  Стартуем именно с неё (2026-07-16). Доступ для публикации/API-токен —
  ещё не прислан (нужен VK access token с правом `wall` для этой страницы,
  переменная `OLEG_VK_TOKEN` в `.env`, см. `gavrik/.env`).
- **Telegram** — канал: https://t.me/textprodv (@textprodv), решено
  2026-07-17.
  - Публикация: через существующего бота Гаврика (`@mayroden_bot`,
    токен `TELEGRAM_TOKEN` уже в `.env` — отдельного бота заводить не
    стали, сознательно приняли риск смешения личного/рабочего бота с
    контент-пайплайном). **Требуется:** добавить `@mayroden_bot`
    администратором канала `@textprodv` с правом публикации сообщений —
    без этого шага публикация технически невозможна (Bot API не может
    постить в канал, где бот не админ).

Оба формата — личный бренд, не студия/группа. Это важно для Стратега:
тон "от первого лица", не "от компании".
