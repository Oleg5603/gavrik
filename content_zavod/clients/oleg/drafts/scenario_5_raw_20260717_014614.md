Using superpowers:using-superpowers — checking if a relevant skill applies. This task is producing a JSON scenario structure per an already-detailed system prompt in the conversation; no code, debugging, or creative brainstorming with the user is involved. This is a direct content-generation task, not one matched by the available skills (brainstorming is for exploring user intent before implementation work, not for following an already-specified content pipeline prompt).

{"problem":"Предприниматель слышит «результат ненадёжный» и представляет ИИ как чёрный ящик без контроля качества", "insight":"На 15+ проектах ни один не встал из-за ненадёжности ИИ — потому что код проверяется тестами и ревью так же, как после любого разработчика; ИИ ускоряет написание, а не отменяет проверку", "conclusion_or_cta":"Ненадёжный не ИИ, а процесс без контроля — методика решает это, а не убирает", "format_notes":"Первые 2-3 сек: сразу называем миф и переворачиваем его («ИИ-разработка ненадёжна» — окей, а вот 15+ проектов без единого срыва). Инсайт показывается визуально/на камеру: скриншот процесса тестов/ревью, конкретный жест «вот так проверяю» — не абстрактное объяснение. Призыв короткий и прямой: «спросите в комментах, как я проверяю код» — без ссылок в теле, провоцирует конкретный вопрос по процессу, а не общее «а как вы думаете»."}